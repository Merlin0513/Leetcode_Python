class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        for i in nums:
            for j in nums:
                if j == target-i and j!=i:
                    return nums.index(i), nums.index(j)
                    
# NOTICE: Leetcode result needs ‘return’ not ‘print’
